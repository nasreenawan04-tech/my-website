I need you to fix all major technical, SEO, and performance issues for my website https://dapsiwow.com based on this audit report.  

# 🟥 CRITICAL ISSUES (Fix Immediately)  
1. **Client-Side Rendering Problems**  
   - Tool pages are stuck at "Loading page content..." due to JavaScript rendering failures.  
   - Implement **Server-Side Rendering (SSR)** or **Static Site Generation (SSG)** (e.g., Next.js / Remix) so tools render content for both users and search engines.  
   - Ensure all tool pages load full HTML output instead of relying only on client-side JS.  

2. **Performance Optimization**  
   - Optimize JavaScript by applying **code splitting** and **lazy loading**.  
   - Reduce bundle size and dependency bloat.  
   - Use Lighthouse/PageSpeed Insights audits to guide changes.  
   - Improve **Core Web Vitals** (LCP, FID, CLS).  

# 🟧 HIGH PRIORITY (Next 2–4 weeks)  
3. **Search Engine Visibility / SEO Recovery**  
   - Fix indexing issues: submit updated sitemap to Google Search Console.  
   - Add **unique title tags, meta descriptions, and H1/H2 tags** for all tool pages.  
   - Implement **structured data (JSON-LD)** for calculators/tools.  
   - Fix soft 404s:  
     - Add proper content to thin pages, OR  
     - Return correct `404` for missing tools, OR  
     - Redirect via `301` to relevant working tools.  
   - Update robots.txt to ensure only important pages are crawlable.  

4. **Mobile Optimization**  
   - Run a full audit with PageSpeed Insights.  
   - Ensure responsive design across all devices.  
   - Compress and optimize images/assets.  
   - Implement a mobile-first CSS strategy.  

# 🟩 MEDIUM PRIORITY (1–3 months)  
5. **Accessibility Compliance**  
   - Run accessibility audit with WAVE.  
   - Add ARIA labels and proper alt text.  
   - Ensure keyboard navigation works for all tools.  
   - Aim for WCAG 2.1 AA compliance.  

6. **Advanced Performance & Security**  
   - Add a **service worker** for offline caching.  
   - Integrate a **CDN** for global performance.  
   - Ensure HTTPS everywhere, with HSTS enabled.  
   - Further reduce bundle size and improve caching policies.  

# 🛠️ EXPECTED OUTCOMES  
- All tools will load correctly and be accessible to users and search engines.  
- Google Search Console will index 180+ tools properly.  
- Performance scores: 70–90+ on PageSpeed.  
- SEO traffic increase of 500–1000%.  
- UX improvements: bounce rate reduced, better engagement.  

Please apply these fixes step by step in the project codebase, updating both **frontend and backend** where necessary.  
